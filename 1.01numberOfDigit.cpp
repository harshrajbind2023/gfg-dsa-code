#include <bits/stdc++.h>
using namespace std;
int main()
{
   int count = 0, num;
   cin >> num;
   while (num != 0)
   {
    
      num = num / 10;
      count++;
      /* code */
   }
   cout << count;
}